{
  "scripts": {
    "dev": "deno run -A --unstable --import-map=./import_map.json ./cli.ts",
    "piwo": "deno run --allow-net --no-check --import-map=./import_map.json ./cli.ts",
    "test": "deno test --import-map=./import_map.json"
  }
}
